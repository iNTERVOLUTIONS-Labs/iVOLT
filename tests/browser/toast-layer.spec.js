    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
{    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
,    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
x    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
}    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
f    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
@    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
y    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
w    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
/    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
/    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
/    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
A    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
D    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
R    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
-    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
0    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
3    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
4    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
:    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
y    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
v    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
,    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
b    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
v    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
/    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
/    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
I    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
y    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
b    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
k    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
b    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
y    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
b    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
w    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
k    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
v    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
y    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
v    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
b    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
y    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
w    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
w    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
b    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
v    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
v    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
v    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
,    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
y    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
{    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
}    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
=    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
>    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
{    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
w    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
/    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
f    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
x    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
/    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
/    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
b    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
w    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
[    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
-    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
v    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
-    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
=    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
]    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
k    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
w    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
x    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
#    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
H    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
v    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
A    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
b    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
,    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
=    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
w    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
v    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
y    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
=    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
>    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
{    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
=    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
w    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
/    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
k    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
/    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
v    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
/    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
/    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
j    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
/    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
/    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
j    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
=    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
E    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
v    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
N    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
=    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
v    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
-    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
-    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
A    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
b    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
-    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
b    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
,    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
N    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
f    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
b    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
y    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
=    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
w    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
T    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
=    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
w    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
{    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
:    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
C    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
,    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
v    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
:    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
,    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
:    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
0    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
}    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
w    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
w    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
P    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
=    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
>    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
q    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
A    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
F    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
=    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
>    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
q    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
A    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
F    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
=    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
B    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
C    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
R    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
=    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
F    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
P    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
f    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
+    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
w    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
/    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
2    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
,    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
+    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
/    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
2    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
{    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
v    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
:    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
A    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
b    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
v    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
,    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
:    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
y    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
f    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
w    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
P    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
v    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
=    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
=    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
=    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
f    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
,    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
b    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
:    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
!    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
!    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
&    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
&    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
,    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
x    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
:    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
f    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
,    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
y    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
:    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
,    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
w    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
:    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
w    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
,    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
:    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
}    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
}    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
f    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
!    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
k    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
,    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
v    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
b    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
x    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
v    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
B    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
x    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
w    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
B    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
G    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
T    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
0    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
x    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
b    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
B    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
w    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
k    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
x    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
+    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
w    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
-    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
1    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
6    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
,    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
y    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
+    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
1    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
6    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
/    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
/    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
b    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
f    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
f    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
=    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
w    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
v    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
=    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
>    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
d    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
m    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
q    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
y    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
S    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
A    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
v    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
-    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
h    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
x    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
f    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
B    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
0    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
w    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
x    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
l    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
c    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
#    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
s    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
g    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
.    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
H    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
a    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
v    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
A    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
r    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
i    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
b    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
u    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
t    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
(    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
o    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
p    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
e    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
n    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
,    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
     const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
"    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
}    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
)    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
;    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});

    const r = item.element.getBoundingClientRect();
    let open = false;
    try { open = region.matches(":popover-open"); } catch {}
    return { popover: region.getAttribute("popover"), supported: typeof region.showPopover === "function", open, x: r.left, y: r.top, w: r.width, h: r.height, vw: innerWidth, vh: innerHeight };
  });
  if (!result.supported) test.skip(true, "popover attribute not supported in this engine");
  expect(result.popover).toBe("manual");
  expect(result.open).toBe(true);
  expect(result.w).toBeGreaterThan(0);
  expect(result.x + result.w).toBeLessThanOrEqual(result.vw);
  expect(result.y + result.h).toBeLessThanOrEqual(result.vh);
  // Closing the dialog leaves the toast in place and dismissible.
  await page.keyboard.press("Escape");
  await expect(page.locator("#signup")).not.toHaveAttribute("open", "");
  await page.locator(".iv-toast .iv-toast__dismiss, .iv-toast button").first().click();
  await expect(page.locator(".iv-toast")).toHaveCount(0);
  const closed = await page.evaluate(() => { try { return document.querySelector(".iv-toast-region").matches(":popover-open"); } catch { return null; } });
  expect(closed).toBe(false);
});
